#include <iostream>
#include <ctime>
using namespace std;
int main()
{
 int x; // variable to store random number

    // Loop with FOR
    // Print the text 5 times
    cout<< "For Loop" << endl;

    for (int i=0;i<=4;i++) // loop runs 5 times (0 to 4)
    {
        cout<< "Information Technology UMY" <<endl; // print text
    }

    cout<<endl;


  // To get a random sequence number
    srand(time(0)); // seed for random numbers

    // Loop with while
    cout<< "Print white" << endl;

    x=1+rand()%10; // generate random number (1–10)
}
